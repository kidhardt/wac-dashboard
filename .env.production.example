# Production Environment Configuration
# Copy this file to .env.production and add your API key

# API Mode: Use direct API calls (no backend server needed)
VITE_USE_DIRECT_API=true

# Anthropic API Key (will be embedded in production build)
# Get your API key from: https://console.anthropic.com/settings/keys
VITE_ANTHROPIC_API_KEY=your-api-key-here

# Production Notes:
# - No backend server required (works on Siteground)
# - API calls go directly to https://api.anthropic.com/v1/messages
# - API key will be visible in browser network requests (acceptable if dashboard is password-protected)
# - Monitor your API usage at: https://console.anthropic.com/settings/usage
# - Consider setting usage limits to prevent abuse
